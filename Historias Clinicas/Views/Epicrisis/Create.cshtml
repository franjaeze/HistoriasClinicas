@model Historias_Clinicas.Models.Epicrisis

@{
    ViewData["Title"] = "Create";
}

<h1>Crear</h1>

<h4>Epicrisis</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*@if (User.IsInRole("Medico"))
            {
                <div class="divBoton">
                    <a type="boton" asp-action="CargarDiagnostico" asp-controller="Epicrisis">Agregar Diagnostico</a>
                </div>
            }*@
            @*<div class="divBoton">
                <a type="boton" asp-action="DiagnosticoPorEpicrisis" asp-controller="Diagnostico">Ver Diagnostico</a>
            </div>*@
            <div class="form-group">
                <input type="submit" asp-route-id="" value="Crear" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a type="button" class="btn btn-secondary" style="margin-left: 20px" asp-action="Index" asp-controller="Pacientes">Cancelar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
